export const AuthorsAddAuthor = () => import('../../../../components/authors/AddAuthor.vue' /* webpackChunkName: "components/authors-add-author" */).then(c => wrapFunctional(c.default || c))
export const Authors = () => import('../../../../components/authors/Authors.vue' /* webpackChunkName: "components/authors" */).then(c => wrapFunctional(c.default || c))
export const BooksAddBook = () => import('../../../../components/books/AddBook.vue' /* webpackChunkName: "components/books-add-book" */).then(c => wrapFunctional(c.default || c))
export const Books = () => import('../../../../components/books/Books.vue' /* webpackChunkName: "components/books" */).then(c => wrapFunctional(c.default || c))
export const FormsLogin = () => import('../../../../components/forms/Login.vue' /* webpackChunkName: "components/forms-login" */).then(c => wrapFunctional(c.default || c))

// nuxt/nuxt.js#8607
function wrapFunctional(options) {
  if (!options || !options.functional) {
    return options
  }

  const propKeys = Array.isArray(options.props) ? options.props : Object.keys(options.props || {})

  return {
    render(h) {
      const attrs = {}
      const props = {}

      for (const key in this.$attrs) {
        if (propKeys.includes(key)) {
          props[key] = this.$attrs[key]
        } else {
          attrs[key] = this.$attrs[key]
        }
      }

      return h(options, {
        on: this.$listeners,
        attrs,
        props,
        scopedSlots: this.$scopedSlots,
      }, this.$slots.default)
    }
  }
}
